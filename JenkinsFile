pipeline{
    agent any
    options {
        timestamps()
        disableConcurrentBuilds()
        timeout(time: 10, unit: 'MINUTES')
        buildDiscarder(logRotator(numToKeepStr: '20'))
    }
    environment {
        IMAGE = "registry.company.com/python-service:${env.GIT_COMMIT}"
    }
    stages{
        stage('checkout'){
            steps{
                checkout scm
            }
        }
    }   
}