pipeline{
    agent {any}
    options {
        timestamps()
        disableCouncurrentBuilds()
        timeout(time: 10, unit; 'MINUTES')
        buildDiscarder(logRotator(numToKeepStr: '20'))
    }
    environment {
        
    }
    stages{
        stage('checkout'){
            steps{
                checkout scm
            }
        }
    }
}